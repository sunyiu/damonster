export enum Deck_com_events {
    Draw = 'draw-from-deck'
}

export enum Player_com_events {
    SetHero = 'set-hero',
    EquipHero = 'equip-hero',
    DoAction = 'do-action',
    SkipAction = 'action-done',
    DoBattle = 'do-battle'
}